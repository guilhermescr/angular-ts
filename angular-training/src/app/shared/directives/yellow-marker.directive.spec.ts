import { YellowMarkerDirective } from './yellow-marker.directive';

describe('YellowMarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new YellowMarkerDirective();
    expect(directive).toBeTruthy();
  });
});
